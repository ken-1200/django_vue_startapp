commands:
  command block:
    command: |
      git clone https://github.com/creationix/nvm.git ~/.nvm
      source ~/.nvm/nvm.sh
      nvm install node
      npm install yarn -g
      yarn global add @vue/cli@3.12.0
      yarn run serve
